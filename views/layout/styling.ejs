<style>
  /* Mengatur ulang beberapa default browser */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Open Sans', sans-serif;
  }

  /* Menggunakan TATA LETAK BARU (flexbox)
    tapi dengan GAMBAR LATAR LAMA Anda 
  */
  body {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    
    /* --- INI DARI CSS LAMA ANDA --- */
    background-image: url(https://wallpapercave.com/wp/ixmt8JQ.jpg);
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    /* ------------------------------ */
  }

  /* Menggunakan KARTU BARU
    tapi dengan WARNA KARTU LAMA Anda 
  */
  .form-container {
    /* --- INI DARI CSS LAMA ANDA --- */
    background-color: rgba(0, 0, 0, .7);
    box-shadow: 0 0 10px rgba(255, 255, 255, .3);
    /* ------------------------------ */

    padding: 2.5rem 3rem;
    border-radius: 10px;
    width: 100%;
    max-width: 400px;
    text-align: center;
  }

  /* Menggunakan JUDUL BARU
    tapi dengan WARNA TEKS LAMA Anda 
  */
  .form-container h1 {
    color: #fafafa; /* <-- Dari CSS Lama */
    margin-bottom: 1.5rem;
    font-size: 2rem;
    text-transform: uppercase;
    border-bottom: 4px solid #2979ff; /* <-- Dari CSS Lama */
    text-align: left;
  }

  /* Grup untuk setiap label + input */
  .form-group {
    margin-bottom: 1.25rem;
    text-align: left;
  }

  /* Menggunakan LABEL BARU
    tapi dengan WARNA TEKS LAMA Anda 
  */
  .form-group label {
    display: block;
    margin-bottom: 0.5rem;
    color: #90caf9; /* <-- Dari CSS Lama */
    font-weight: 600;
  }

  /* Ini adalah INPUT FIELD BARU (Putih),
    sesuai permintaan Anda 
  */
  .form-group input[type="text"],
  .form-group input[type="password"],
  .form-group input[type="email"] {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid #ddd;
    border-radius: 5px;
    font-size: 1rem;
    background: #fff; /* Pastikan putih */
    color: #333;      /* Teks di dalam input jadi hitam */
  }

  /* Styling untuk tombol (BARU) */
  .form-button {
    width: 100%;
    padding: 0.85rem;
    border: none;
    border-radius: 5px;
    background-color: #2979ff; /* Samakan dengan border h1 */
    color: white;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  .form-button:hover {
    background-color: #0056b3;
  }

  /* Menggunakan "Show Password" BARU
    tapi dengan WARNA TEKS LAMA Anda 
  */
  .show-password {
    display: flex;
    align-items: center;
    margin-top: 0.5rem;
    font-size: 0.9rem;
    color: #90caf9; /* <-- Dari CSS Lama */
  }

  .show-password input {
    margin-left: 0.5rem;
  }
  
  .show-password label {
    margin: 0;
    margin-left: 0.25rem;
    font-weight: 400;
    color: #90caf9; /* <-- Dari CSS Lama */
  }

  /* Menggunakan LINK BARU
    tapi dengan WARNA TEKS LAMA Anda 
  */
  .form-link {
    display: block;
    margin-top: 1.5rem;
    color: #ffffff; /* <-- Dari CSS Lama */
    text-decoration: none;
  }

  .form-link:hover {
    text-decoration: underline;
  }
</style>